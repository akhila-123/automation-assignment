cache:
  paths:
    - node_modules/
    - /tmp/.cache/Cypress

stages:
  - setup
  - test

setup:
  stage: setup
  image: node:12.16.1
  script:
    - npm install

e2e:chrome:
  stage: test
  image: trion/ng-cli-e2e
  script:
    - npm install
    - npm run test
